class Solution {
    public String minWindow(String s, String t) {
        int len_s = s.length();
        int len_t = t.length();
        if(len_s < len_t) {
            return "";
        }
        int min = 100001;
        
        return "";
    }
}