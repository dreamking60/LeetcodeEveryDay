/**
 * struct Point {
 *	int x;
 *	int y;
 *	Point(int xx, int yy) : x(xx), y(yy) {}
 * };
 */
class Solution {
    public:
        /**
         * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
         *
         * // 主函数：输入小矩形 (w,h)，返回最大面积和
         * @param rects Point类vector rects 为小矩形列表，宽高分别为Point.x 和Point.y
         * @return int整型
         */
        int maxRectArea(vector<Point>& rects) {
            // write code here
        }
    };